export function startCronJobs(): void {
  // Placeholder: il motore di trading verra' collegato qui.
  // Evita errori di import durante la fase iniziale auth.
  console.log('[jobs] Trading engine cron non ancora attivo');
}
